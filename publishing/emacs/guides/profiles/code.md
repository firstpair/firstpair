## Using a code edition

A code edition links prose to exact source excerpts. Each excerpt in the
references manual names its file, its line range, and the revision it was
taken from; `C-c C-f` opens the delivered file itself in the lower window.
The references manual is a reading aid, not a build tree: rebuild the
software from the repository the colophon names.
